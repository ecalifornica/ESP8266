const char* SSID = "network name";
const char* PASSWORD = "password";
const char* HOST = "www.host.com";
const char* PORT = "8080"; // Should this be stored as an int and cast?
const char* HOST_PORT = HOST + PORT; // How do I actually do this?
String URL = "/api/";
